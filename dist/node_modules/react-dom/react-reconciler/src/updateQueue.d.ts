import { Action } from 'packages/shared/ReactTypes';
export interface Update<State> {
    action: Action<State>;
}
export interface UpdateQueue<State> {
    shared: {
        pending: Update<State> | null;
    };
}
export declare const createUpdate: <State>(action: Action<State>) => Update<State>;
export declare const createUpdateQueue: <State>() => UpdateQueue<State>;
export declare const enqueueUpdate: <State>(updateQueue: UpdateQueue<State>, update: Update<State>) => void;
export declare const processUpdateQueue: <State>(baseState: State, pendingUpdate: Update<State>) => {
    memoizedState: State;
};
