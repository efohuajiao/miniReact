(function (global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = factory(require('packages/shared/ReactSymbols')) :
  typeof define === 'function' && define.amd ? define(['packages/shared/ReactSymbols'], factory) :
  (global = typeof globalThis !== 'undefined' ? globalThis : global || self, (global.index = global.index || {}, global.index.js = factory(global.ReactSymbols)));
})(this, (function (ReactSymbols) { 'use strict';

  const ReactElement = (type, key, ref, props) => {
      const element = {
          $$typeof: ReactSymbols.REACT_ELEMENT_TYPE,
          type,
          key,
          ref,
          props,
          __mark: 'Huajiao'
      };
      return element;
  };
  const jsxDEV = (type, config) => {
      let key = null;
      const props = {};
      let ref = null;
      for (const prop in config) {
          const val = config[prop];
          if (prop === 'key') {
              if (val !== undefined) {
                  key = '' + val;
              }
              continue;
          }
          if (prop === 'ref') {
              if (val !== undefined) {
                  ref = val;
              }
              continue;
          }
          if ({}.hasOwnProperty.call(config, prop)) {
              props[prop] = val;
          }
      }
      return ReactElement(type, key, ref, props);
  };

  var index = {
      version: '0.0.0',
      createElement: jsxDEV
  };

  return index;

}));
