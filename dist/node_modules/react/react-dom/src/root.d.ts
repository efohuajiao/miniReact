import { Container } from './hostConfig';
import { ReactElementType } from 'shared/ReactTypes';
export declare function createRoot(container: Container): {
    render(element: ReactElementType): void;
};
